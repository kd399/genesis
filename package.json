{
  "name": "genesis",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "functions"
  ],
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "emulators": "firebase emulators:start",
    "deploy": "firebase deploy",
    "format": "npx prettier --write '**/*.{ts,vue,js,json,css,md}'"
  },
  "devDependencies": {
    "@prettier/plugin-xml": "^3.4.2",
    "prettier": "^3.9.6"
  },
  "dependencies": {
    "firebase-functions": "^7.3.2"
  }
}
